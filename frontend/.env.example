# Frontend dev server port
VITE_PORT=3000

# API base URL used by React (use /api with Vite proxy in development)
VITE_API_URL=/api

# Backend URL for Vite dev proxy (not exposed to browser)
VITE_API_PROXY_TARGET=http://localhost:5000
